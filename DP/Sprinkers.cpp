#include<bits/stdc++.h>
using namespace std;
int main(){
    int n,q;    cin >> n >> q;
    vector<int> pos(n),range(n);
    for(int i = 0;i < n; i++){
        // of the form (x,0)
        cin >> pos[i];
    }
    for(int i = 0;i < n; i++){
        cin >> range[i];
    }
    
    return 0;
}